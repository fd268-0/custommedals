namespace CustomMedals {
    /*
     * Returns the currently resolved custom medals as a JSON array string.
     *
     * Each item has:
     * - name: string
     * - time: int
     * - iconColor: string
     * - icon: string
     * - isPb: bool
     */
    import string GetCustomMedalsJson() from "CustomMedals";
}
